import React from "react";

import {LoginPage} from "../pages";

const App = () => {
    return (
        <main className="jumbotron">
            <h1>App</h1>
            <LoginPage />
        </main>
    );
};

export default App;
